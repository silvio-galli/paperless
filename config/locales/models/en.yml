en:
  activerecord:
    models:
      product:
        one: "product"
        other: "products"
      customer:
        one: "customer"
        other: "customers"
      order:
        one: "order"
        other: "orders"
    # VALIDATION ERRORS
    errors:
      models:
        product:
          arriving_date: "cannot be blank if product has \"arriving\" status."
