en:
  activerecord:
    models:
      customer:
        one: "customer"
        other: "customers"
      product:
        one: "product"
        other: "products"
      order:
        one: "order"
        other: "orders"
    # VALIDATION ERRORS
    errors:
      models:
        product:
          arriving_date: "cannot be blank if product has \"arriving\" status."
